import 'package:flutter/material.dart';

/// Global key for the header wishlist icon — used by fly-to-wishlist animation.
final GlobalKey wishlistNavIconKey = GlobalKey(debugLabel: 'wishlistNavIcon');
